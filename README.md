# WunCMS / cms-fullpagetext

## Instalation

`composer require wundermanpraha/cms-fullpagetext`

Run [DB migration] (database/schema.sql).

## Minimal configuration

```yaml
extensions:
	CMSFullPageText: Wunderman\CMS\FullPageText\DI\FullPageTextExtension
```